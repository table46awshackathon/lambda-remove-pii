{
  "name": "lambda-remove-pii",
  "version": "1.0.0",
  "description": "Removes PII from data",
  "main": "index.js",
  "scripts": {
    "create": "claudia create --region us-west-2 --handler index.handler --no-optional-dependencies --set-env PII_KEY_PREFIX=pii,NOPII_KEY_PREFIX=clean && sleep 1 && claudia add-s3-event-source --bucket table46hackathondata2 --prefix pii",
    "update": "claudia update",
    "test-lambda": "claudia test-lambda --event sample-s3-event.json",
    "test": "sam local invoke --event sample-s3-event.json"
  },
  "author": "",
  "license": "ISC",
  "optionalDependencies": {
    "aws-sdk": "^2.157.0"
  },
  "dependencies": {
    "csv-parse": "^2.0.0",
    "lodash": "^4.17.4"
  }
}
